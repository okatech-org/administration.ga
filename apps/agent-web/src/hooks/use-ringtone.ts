// Re-export from the shared package so every consumer references the same implementation.
export { useRingtone } from "@workspace/agent-features/hooks"
export type { RingtoneVariant } from "@workspace/agent-features/hooks"
